server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API calls go directly to the backend service URL from the browser
    # Configure VITE_API_URL env variable in your frontend build
}
